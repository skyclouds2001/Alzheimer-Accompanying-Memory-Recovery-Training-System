<view class="t"><text>随时随地在线预约</text></view>
<view class="container">
    <view wx:for="{{ array }}" wx:key="id" class="content_item">
      <view class="item">
        <image src="{{ item.coverImgUrl }}"></image>
        <text>{{ item.name }}</text>
    </view>
  </view>
</view>
<view class="h2"><text>专家号</text></view>
<view class="items" data-index="{{index}}" bindtap="jumpto" wx:for="{{login_obj}}" wx:key="whos">

<image src="{{item.img_src}}" class="icon"></image>
<view class="kong"></view>
<view class="left">
<view class="h1">{{item.whos}}医生</view>
<view class="describe">{{item.describe}}</view>

<view class="buttom">
<view class="time" ><van-icon name="user-o" custom-class="icon1" size="18" />{{title}}:</view>
<view class="kong2"></view>
<view class="ico2">
<van-icon custom-class="icon" name="records"  size="16" color="#f07e78" />
<view class="tt">从医30年</view>
 </view>
</view>

</view>
</view>

