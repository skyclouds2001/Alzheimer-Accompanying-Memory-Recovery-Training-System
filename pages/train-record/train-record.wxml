<block wx:for="{{ records }}" wx:key="id">
  <timeline color="{{ item.color }}" date="{{ item.date }}" type="{{ item.type }}" time="{{ item.time }}" direction="{{ item.direction }}" zindex="{{ item.zindex }}"></timeline>
</block>
