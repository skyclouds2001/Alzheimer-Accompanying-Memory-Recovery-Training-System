<block wx:for="{{ trainRecord }}" wx:key="id">
  <timeline color="{{ item.color }}" date="{{ item.date }}" text="{{ item.text }}" direction="{{ item.direction }}" zindex="{{ 100 - item.id }}"></timeline>
</block>
