
<view class="tab_new">

<view class="tab_elements {{index===select_index?'selected':''}}" wx:for="{{element_list}}" wx:key="title">

<navigator url="{{item.url}}" open-type="redirect">{{item.title}}</navigator>

</view>

</view>